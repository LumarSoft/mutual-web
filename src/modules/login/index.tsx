import { LoginCard } from "./components/LoginCard";

const LoginModule = () => {
  return (
    <main className="w-screen h-screen bg-background flex items-center justify-center flex-col">
      <LoginCard />
    </main>
  );
};

export default LoginModule;
